export type FilterTypes = "dropdown" | "range" | "checkbox";

export const EnumFilterTypes = {
    DROPDOWN: "dropdown" as FilterTypes,
    RANGE: "range" as FilterTypes,
    CHECKBOX: "checkbox" as FilterTypes,
}
